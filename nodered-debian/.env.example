# Copiar este archivo a ".env" (sin .example) y completar antes de "docker compose up -d"
# El archivo ".env" real NO se sube al repositorio (ver .gitignore)

INFLUXDB_USER=admin
INFLUXDB_PASSWORD=cambiar_esta_clave
INFLUXDB_TOKEN=cambiar-este-token-por-uno-propio

GRAFANA_USER=admin
GRAFANA_PASSWORD=cambiar_esta_clave
